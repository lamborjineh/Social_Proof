# ─────────────────────────────────────────────────────────────
# SocialProof — Python Backend Dependencies  v3.1
# Tested against Python 3.11.9
# Install: pip install -r requirements.txt
# Then:    python -m spacy download en_core_web_sm
#          playwright install chromium
# ─────────────────────────────────────────────────────────────

# API Framework
fastapi==0.110.0
uvicorn[standard]==0.29.0

# NLP — spaCy (locked to avoid CLI crash)
spacy==3.7.4
typer==0.9.0
click==8.1.7

# Semantic Similarity
sentence-transformers==2.6.1

# NLI / Transformers
# Active model: MoritzLaurer/mDeBERTa-v3-base-mnli-xnli (multilingual, ~280MB)
# Replaces roberta-large-mnli (English-only, 1.4GB)
transformers==4.40.0
torch==2.2.2
torchvision==0.17.2

# HuggingFace hub — model downloads (used directly in retrieval/model code)
huggingface_hub>=0.23.0,<2.0

# Database
sqlalchemy==2.0.29
pymysql==1.1.1

# Password hashing
bcrypt==4.1.3

# Utilities
pydantic==2.6.4
python-multipart==0.0.9
numpy==1.26.4
python-dotenv==1.0.1
email-validator

# FAISS + BGE-M3 Corpus Retrieval
faiss-cpu==1.8.0
FlagEmbedding==1.2.5

# Corpus scraper + live search
requests==2.31.0
beautifulsoup4==4.12.3
trafilatura==2.0.0

# Async HTTP — Google Fact Check API integration
httpx==0.27.0

# MBFC sync — iffy.news CSV parsing
pandas==2.2.2

# Image processing — used across OCR and file pipelines
Pillow==12.1.1

# OCR — image input pipeline (pipeline/image_input.py)
# easyocr 1.7.2 pulls torchvision; install after base torch is confirmed working
easyocr==1.7.2
pytesseract==0.3.13

# PDF file submission support
pdfplumber==0.11.0

# Word / PowerPoint file submission support (pipeline/file_input.py)
# python-docx and python-pptx are NOT in the base pip list — run pip install after setup
python-docx==1.1.2
python-pptx==1.0.2

# Live search
cloudscraper==1.2.71
playwright==1.59.0
playwright-stealth==2.0.3
# After install run: playwright install chromium

# Redis — persistent rate limiter + JWT token blocklist (routers/auth.py)
# Required for multi-worker deployments. Falls back to in-process memory if not set.
# Set REDIS_URL in .env to activate (e.g. redis://localhost:6379/0)
redis==7.4.0
